console.log("Loading...");

document.write("Well, hello World with Webpack!");

// run $ webpack ./app.js bundle.js
// first arg is the input file on what webpack runs, the second arg 
// is where we are putting the output, will create the second arg file  

// add watch to watch for some changes and will rebundle and do a production build
// run $webpack ./app.js bundle.js --watch
